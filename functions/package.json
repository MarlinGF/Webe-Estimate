{
  "name": "estimates-functions",
  "engines": {
    "node": "20"
  },
  "scripts": {
    "build": "echo \"No build needed\"",
    "deploy": "firebase deploy --only functions:estimatesApi"
  },
  "dependencies": {
    "firebase-admin": "^12.0.0",
    "firebase-functions": "^5.1.0",
    "cors": "^2.8.5",
    "express": "^4.18.2"
  }
}
